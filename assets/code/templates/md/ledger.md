# __NAME__ — Ledger

Append-only log. Newest entries on top. ISO-8601 dates. Never delete.

---

## __DATE__ — Project registered with AntCrate

Registered as `__NAME__` (domain `__DOMAIN__`). Home orchestration layer at `~/CLAUDE.md` now governs structural ops; project-specific guidance lives in this project's `CLAUDE.md`.

Going forward:
- `antcrate --backup __NAME__` before any structural change.
- `antcrate --commit __NAME__ -m "..."` to commit.
- `antcrate --pp __NAME__` to push (commits + triages on rejection).
